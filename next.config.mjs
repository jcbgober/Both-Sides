JavaScript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add any future tweaks here, e.g., images: { domains: ['example.com'] }
};

export default nextConfig;
